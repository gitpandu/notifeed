services:
  notifeed:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: notifeed
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_PATH: /data/notifeed.db
    volumes:
      - data:/data

volumes:
  data: